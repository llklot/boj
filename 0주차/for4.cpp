#include <bits/stdc++.h>
using namespace std;
int n;
int main(){
    cin >> n;
    vector<int> a(n, 0);
    //vector<자료형> 변수명(개수, 초기값)
    for(int i : a){
        cout << i << " ";
    }
    return 0;
}